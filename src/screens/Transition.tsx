/**
 * Placeholder neutro para los estados transitorios T_UNIT_ENTER / T_RETURN.
 * Motion real (glass compression, yellow energy, frosted→clear) es TASK 005.
 */
export function Transition() {
  return <div className="screen screen-transition" aria-hidden="true" />;
}
